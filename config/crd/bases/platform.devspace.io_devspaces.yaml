---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: devspaces.platform.devspace.io
spec:
  group: platform.devspace.io
  names:
    kind: DevSpace
    listKind: DevSpaceList
    plural: devspaces
    singular: devspace
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DevSpace is the Schema for the devspaces API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DevSpaceSpec defines the desired state of DevSpace
            properties:
              foo:
                description: Foo is an example field of DevSpace. Edit devspace_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: DevSpaceStatus defines the observed state of DevSpace
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
